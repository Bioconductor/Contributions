% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distances_n_other.r
\name{savePhylipDistance}
\alias{savePhylipDistance}
\title{Save Distance Matrix in PHYLIP Format}
\usage{
savePhylipDistance(PhylipFile_name, distance, mode = "relaxed")
}
\arguments{
\item{PhylipFile_name}{Output file name (string).}

\item{distance}{Matrix of pairwise distances.}

\item{mode}{Character. Either "relaxed" (default) or any other string for strict format.}
}
\value{
None. Writes a file to disk.
}
\description{
Writes a pairwise distance matrix in PHYLIP format, relaxed or strict.
}
\examples{
# Create a sample 3x3 distance matrix with row and column names
dist_matrix <- matrix(c(
  0, 1.2, 2.3,
  1.2, 0, 1.5,
  2.3, 1.5, 0
), nrow = 3, byrow = TRUE)
colnames(dist_matrix) <- rownames(dist_matrix) <- c("seq1", "seq2", "seq3")

# Save the distance matrix in PHYLIP relaxed format to a temp file
tmp_file <- tempfile(fileext = ".phy")
savePhylipDistance(tmp_file, dist_matrix, mode = "relaxed")

# Check file contents (optional)
cat(readLines(tmp_file), sep = "\n")

}
